import styles from '../styles/Home.module.css'

export default function Length (props) {
  return (
    <div className={styles.card}>
      <code className={styles.code}>Length</code>
    </div>
  )
}